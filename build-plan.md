# DevNetwork [API + Cloud + AI] Hackathon 2026 — Build Plan
 
**Status:** registered, online phase running. First batch landed Aug 18 (ahead of its window); **Gate 0 landed Aug 18 — eSign 5/5 PASS, Nutrient Data Extraction blocked on a missing product key** (see `docs/gate0-aug18.md`). Plan revised Aug 18 (`docs/review-aug18.md`). **Aug 20: Nutrient solutions engineer (Jon Addams) engaged with extraction-routing guidance (see `docs/nutrient-support-aug20.md`). `safe-write-mcp-core@0.3.0` published — closes the `/api/plans` payload leak (issue #18, PR #19); consumed here by version bump. Aug 20: the Nutrient stage LANDED six days early (`docs/nutrient-stage-aug20.md`) — extraction routing, staged redaction, the Nutrient MCP server, 92/92 tests green. It corrected the plan (match labels live on `/extraction/extract`, not `/parse`) and turned up the finding the demo now rests on: on the first live call, every grounding signal cleared two wrong dollar amounts and only `recognitionScore` dissented — and agentic mode, at double the cost, emits no `recognitionScore` at all. Aug 27: Foxit literal-compliance revision — plan extended to start from a plain prompt and end with a retrieved signed document, with real Foxit PDF Services assembly wired before the gate (see § "The submission" and day-by-day Aug 29–30). Aug 27, later: Foxit sponsor contact answered (Jason Welch, Senior Strategic Alliance Manager — `docs/foxit-contact-aug27.md`). Three corrections landed in the plan: `pdf_generation` does not exist in the MCP catalog (it is `pdf_from_html`); signed state is the `folder_executed` webhook + `GET /esign/api/v1/folders/document/download?folderId=&docNumber=` (de-speculating cut-list #1); and `SHARED` means "sent," never "signed." Sponsor-side validation of the keep-signing-outside-MCP pattern, in writing.**
**Hard deadline:** Sept 3, 2026, 10:00am PDT = **2:00pm Argentina time**. Not "end of day Sept 3."
**Time budget assumed:** ~60 hours (a few full days plus evenings) across Aug 18 – Sep 2. *Revised Aug 18: the day-by-day now sums to ~62h of coding plus ~5h of PR/review overhead plus an unestimated Aug 31 freeze day — call it **~70h against a ~60h budget**. Revised Aug 27: five Foxit literal-compliance pivots add ~12h (A2+B4+C2+D2+E2) but reuse the same gate/core, so net ~82h vs ~60h budget — gap absorbed by ordered cut list and by making Nutrient strictly optional for the Foxit track. The gap is real. It is absorbed by the cut list, which is why the cut list is ordered and why the never-cut items are now four (crash-safety, real Foxit PDF wiring, plain-prompt entry, demo video).*
**Tracks targeted:** Foxit + Nutrient DWS + Overall. One project, three entries — the rules explicitly allow entering as many challenges as you want, and allow stacking sponsor prizes with Overall. **Foxit is now the primary track for literal compliance; Nutrient remains load-bearing for the second story but is no longer a hard dependency for the Foxit pipeline (single-prompt-to-signed-document must reproduce with only Foxit credentials).**
 
---
 
## What changed since the original pitch
 
The original pitch named **Nutrient + Doctavian**, and aimed them at portfolio projects #1 (RAG) and #3 (triage agent), neither of which is built. Two things have moved since:
 
1. **Foxit published its challenge** (it was "coming soon" at the time of that pitch). It is titled *"Your Agent Shouldn't Sign That"* and it is, almost verbatim, the safe-write pattern: their open-source MCP server exposes ~40 tools for *reversible* document work and **deliberately excludes signing**; to actually sign, the agent must cross a boundary into the eSign API and a human must be involved. **The design of that agent→human handoff is the graded artifact.** That is a better fit than Doctavian and it fits something you have already built.
2. **Servers A and C are done, and the core is already extracted** (`jpka/safe-write-mcp-core`). The fastest credible submission is no longer "build the RAG project"; it's "point the existing kernel at an irreversible action."
 
The original pitch's architectural warning still stands and is now nearly free: *keep the vendor behind an interface so the portfolio version can swap it out.* `safe-write-mcp-core` already has that seam — preview logic is host-supplied, the core only owns the plan lifecycle. Foxit and Nutrient become adapters, not dependencies.
 
**Doctavian is dropped.** Its brief overlaps Foxit's (agent → generated document → signature) but Foxit's is the one that grades the safety boundary, and doing both would split the demo.
 
---
 
## The submission

**One sentence:** an agent that goes from a plain prompt to a signed document, where every reversible step runs unattended and the one irreversible step — sending for signature — is stopped at a human approval gate that survives a process crash without double-sending.

**Pipeline (Aug 27 literal-compliance revision — every step maps to a Foxit rubric line):**

0. **Plain prompt in (PLANNED Aug 29–30 A).** Single natural-language line — e.g. `Take this freight invoice, redact the PII, and send it to Alice and Bob for signature.` — is the only entry point the judge needs. A deterministic parser (PLANNED `mcp/foxit/prompt-parser.mjs`, LLM + zod schema + fallback regex) extracts `{folderName, recipients, docSource, instructions}` into the typed `EsignPayload` that `agent/esign-agent-loop.mjs` already takes. The parsed fields are shown back in the approval card for human correction before any irreversible step; `agent/esign-agent-loop.mjs` will gain a `--prompt "..."` / `prompt` arg and `mcp/foxit/esign-adapter.mjs` keeps its typed payload — the parser is the only new boundary. Until it lands, the structured `runAgentLoop({folderName, recipients})` at `agent/esign-agent-loop.mjs:121` remains the tested entry point.
1. Messy input document — `docSource` resolved from prompt (plus optional upload) into bytes via task B's Foxit assembly *before* the gate; Pipeline step 3 is where those bytes are produced — step 1 is just the logical source, not a separate temporal step. Until B lands, the fixture at `mcp/nutrient/messy-pdf.mjs` remains the fallback bytes.
2. **Nutrient DWS — optional for Foxit, required for Nutrient track.** Data extraction; routing decided on the per-field match label first (`fuzzy_match` / `not_found` go to the human regardless of the confidence number), composite confidence second, then grounding and OCR-recognition floors as vetoes (Aug 20 Nutrient SE guidance, `docs/nutrient-support-aug20.md`; live findings in `docs/nutrient-stage-aug20.md`). Thresholds are **per document type and still to be calibrated** — every entry is marked `calibrated: false` and the uncalibrated default is deliberately strict, so this is not yet an approval policy anyone should treat as tuned. Then PII redaction via the stage-then-apply flow, so redaction gets the same review checkpoint. *(This is Nutrient's own suggested scenario #5, and it makes DWS load-bearing rather than decorative — which is their stated bar: "for at least one core document operation, meaningfully.")* For the Foxit-only reproduction path this step is skipped — the pipeline still gates correctly without it.
3. **Foxit's MCP server — now wired, not stubbed (PLANNED Aug 29–30 B).** Assembly, conversion, OCR, merge via `@foxitsoftware/foxit-pdf-api-mcp-server` (40 tools, verified no signing tool — `mcp/foxit/list-tools.mjs`). Replaces the `tinyPdf` base64 stub at `mcp/foxit/esign-adapter.mjs:createEsignFolder` (`tinyPdf` const, ~line 435) with at least one real reversible call: render the invoice data as HTML, then `pdf_from_html` → `get_task_result` via `mcp/foxit/call-tool.mjs` stdio transport (`pdf_from_url` also works; `pdf_merge` only for combining existing PDFs; the Document Generation REST API covers template-from-JSON with the same credential pair). *(Aug 27 Foxit contact: `pdf_generation` does NOT exist in the catalog — corrected from the plan's earlier guess.)* Output bytes become the `base64FileString` for `createfolder` and its SHA-256 becomes `extra.documentSha256` for the gate's digest line. Includes base64 extraction + polling of `get_task_result` (Gate 0 smoke pattern). All reversible, all unattended. Single credential pair reproduces this — no Nutrient key required: `FOXIT_CLIENT_ID`/`FOXIT_CLIENT_SECRET` (or aliases `FOXIT_CLOUD_API_CLIENT_ID`/`FOXIT_CLOUD_API_CLIENT_SECRET` at `mcp/foxit/esign-adapter.mjs:gatewayHeaders` ~line 59-61 and `mcp/foxit/call-tool.mjs:26-30`) share the same secret for both eSign gateway and PDF Services (per Gate 0 `docs/gate0-aug18.md:28-34`); host overrides are `FOXIT_ESIGN_HOST` (gateway `na1.fusion.foxit.com` at `mcp/foxit/esign-adapter.mjs:35-36`) vs `FOXIT_CLOUD_API_HOST` (PDF-only at `mcp/foxit/call-tool.mjs:30`).
4. **The gate.** Agent proposes an eSign send. The plan preview shows prompt excerpt, parsed recipients, document digest (SHA-256 of the Foxit-assembled bytes), and an explicit irrevocability warning. Human approves or rejects. This is the graded artifact the challenge names — `renderEsignPlan` at `agent/esign-agent-loop.mjs:renderEsignPlan` (~line 48) already renders this way.
5. **Foxit eSign API called directly with client-side dedup** (not via MCP): the plan token keys a durable ledger, and a `folderStatus` reconciliation (DRAFT vs SHARED) ensures the send happens exactly once — then a hash-chained append-only audit record. Credentials are the agent's own `client_id`/`client_secret` headers at `mcp/foxit/esign-adapter.mjs:gatewayHeaders` (~line 58-61) — same pair as PDF Services, as Gate 0 proved.
6. **Signed document out (PLANNED Aug 29–30 C, gated on probe).** `SHARED` means **sent, not signed** — the audit log and demo script must never call SHARED a completed signature. Signed state is signaled by the `folder_executed` eSign webhook (fires after all required signing completes and digital signatures apply), routed through the existing `handleWebhookEvent` dedup at `mcp/foxit/esign-adapter.mjs:650`. Download the completed envelope via `GET /esign/api/v1/folders/download?folderId=` (single document: `GET /esign/api/v1/folders/document/download?folderId=&docNumber=`), with `GET /esign/api/v1/folders/myfolder?folderId=` as the polling and restart-reconciliation fallback — all three routes named by Foxit (Aug 27 contact, `docs/foxit-contact-aug27.md`). Implement as `pollUntilSigned`/`downloadSignedDocument` in `mcp/foxit/esign-adapter.mjs` reusing `checkFolderStatus`/`handleWebhookEvent` (`mcp/foxit/esign-adapter.mjs:650`), with fixture-seam tests on a committed SHARED fixture and only the manual probe hitting live (per Working Constraints #1–#2, same 30s timeout/`transportError` handling as `mcp/foxit/esign-adapter.mjs:77-83`). Until the probe lands, "signed document out" gracefully degrades to "sent for signature" — which is why this is cut-list #1. The probe is a **PLANNED extension** to `mcp/foxit/esign-probe.mjs` (currently only `--create-draft` at line 42) — e.g. `--probe-download` hitting `document/download?folderId=&docNumber=` after self-sign with two owned inboxes and confirming a `folder_executed`-compatible terminal state — to be added before C is assigned; docs referencing it are forward-looking, not runnable today. *Aug 27: the route guesswork is gone (vendor-named), but the live probe is still the gate.*

**Foxit literal-compliance note.** The challenge invites `You can also argue with us. If you think signing belongs in the agent's toolset... build it your way and defend the choice.` Our defense is: signing does NOT belong in the MCP catalog because it is the only irreversible step — the 40-tool catalog is intentionally reversible, and moving signing into it would collapse the safety boundary the challenge grades. The gate is the boundary. That argument is implemented, not just stated: the MCP server is not modified to add a signing tool, and `mcp/foxit/esign-mcp-server.mjs` keeps signing outside MCP.
 
**Why this can win Overall, not just a sponsor track.** Overall is judged on Progress, Concept, and Feasibility. Concept: agents are being handed write access to systems where actions cannot be undone, and the industry's answer is "add a confirmation prompt." Feasibility: this is already two shipped servers and a published npm core — the commercial story is not hypothetical, it's your Upwork listing.
 
---
 
## The technical finding that becomes the story
 
A subagent read the whole core (73 tests, all passing, clean build) and found one gap that matters here, and it is worth understanding precisely because it's both a real bug and the best narrative you have.
 
`consume()` is **causally disconnected from the external side effect**. It flips `entry.used = true` and emits an `"executed"` audit event, and then returns — the host makes the actual API call somewhere the core cannot see. Both orderings fail on a crash:
 
- **Consume, then call:** process dies in between → audit log says the document was sent; it wasn't. Silent under-execution with a lying audit trail.
- **Call, then consume:** process dies in between → the signer already has the email, the core never recorded it, the plan store is in-memory so a restart forgets the token entirely, and a retry **re-sends the document**.
 
`DECISIONS.md` defends the in-memory store on the grounds that "a restart invalidates all pending plans, so nothing executes without re-previewing, which is exactly the safe direction." **That reasoning is correct for Postgres and exactly backwards here.** With rollback available, the dangerous window is before execution. With a send-and-you-can't-unsend API, the dangerous window straddles the call — which the core does not model at all.
 
Also: no idempotency-key contract, no persisted state, `dataDigest` is advisory rather than core-enforced (so a payload referencing a `documentId` passes fingerprint validation even if the document behind it changed — a TOCTOU hole precisely where the document content is the entire risk surface).
 
**This is the demo.** Not the happy path. The 30 seconds that wins is: kill the process mid-send, restart it, show it does not double-send and the audit log is honest about what happened. Nobody else in this hackathon will demo a crash.

### The second finding, added Aug 20: the gate catches a lie the API tells confidently

The crash story is about durability. This one is about why a gate has to exist at all, and it came out of the first live extraction call (`docs/nutrient-stage-aug20.md`).

Given a messy invoice reading `Total due $86.86`, Nutrient's `understand` mode returned **`total_amount: 26.86`** — with `match: "id_match"`, `confidence: 0.970`, and `groundingScore: 0.95`. Every signal a reasonable integrator would threshold on said *auto-approve*. The number was wrong. Only `recognitionScore` (0.678, the second-lowest on the page) dissented, because it is the only signal measuring whether the glyphs were read correctly rather than whether the value came from the right region of the page.

Then `agentic` mode — double the cost, VLM-augmented — got the totals right and silently mangled the line-item table instead, and **emits no `recognitionScore` at all**. Paying more bought better answers and less ability to tell whether they were right.

That is a second 30 seconds, and it is arguably the more commercially legible one: *here is a document, here is the number the AI extracted with 97% confidence, here is what the document actually says, and here is the gate refusing to sign it.* No crash required. The industry's answer to this is a confirmation prompt showing the same confident wrong number.

Both findings point the same way: the signals that tell you an action is safe are not the same signals that tell you an action is correct, and a gate that conflates them is decoration.
 
---
 
## Working constraints (added Aug 18 after the first batch — read before delegating)
 
1. **Agent sandboxes cannot reach the vendor APIs — *unless the sandbox runs on a networked machine*.** Verified in the Aug 18 sandbox: `na1.fusion.foxit.com` and `api.nutrient.io` were unreachable; only `registry.npmjs.org` was allowlisted. **Gate 0 (Aug 18) ran from a networked machine where all three hosts answered** — live probes were executed directly and their transcripts committed. Rule: *test connectivity first; if the host answers, execute and commit the transcript; if not, hand the probe to the human.* Every live-API check remains either way.
2. **Therefore every vendor adapter gets a fixture seam on day one.** Agents test against recorded transcripts. Each stage ships one `node mcp/<vendor>/<stage>-probe.mjs` you run in a single command, with its output committed as the fixture. That is also judge-facing reproducibility for free.
3. **`safe-write-mcp-core` stays its own repo** (it is published; servers A and C consume it) and is checked out alongside `No Undo`. Two PR streams, one `AGENTS.md` workflow. Core v0.2 lands there and is consumed here by version.
4. **Budget the process.** `AGENTS.md` mandates PR → CI → review round → merge for any session that changes files; PR #1 cost one CodeRabbit round. Assume **~40 min per batch**, ~5h across the ~8 remaining batches. That is coding time you do not have — it is counted in the budget below. If it starts hurting, the decision to make is whether `AGENTS.md` should carve out docs-only changes; do not silently skip the ceremony instead.
 
---
 
## Day-by-day
 
**Aug 18–19 — evenings, ~6h. Unblock everything. — ✅ DONE Aug 18, a day early (see `docs/aug18-19.md`)**
- Foxit developer account at `developer-api.foxit.com` (self-serve, instant), Nutrient DWS free tier (no credit card). **Accounts + keys in `.env`.**
- Get Foxit's MCP server running locally against your own client. Confirm the toolset. **Done: `@foxitsoftware/foxit-pdf-api-mcp-server` runs against our keys, 40 tools listed, no signing tool, end-to-end `pdf_from_url` → `get_task_result` smoke test passed.**
- **Verify whether Foxit eSign and Nutrient DWS support idempotency keys.** This blocks the core work below. If eSign has no idempotency header, the fallback is a client-side dedupe ledger keyed on the plan token — decide this before writing code, not during. **Decision locked: Foxit eSign has NO server-side idempotency (billing-only `request_id` dedup; sends deduped by folder `folderStatus DRAFT/SHARED` reconciliation). Nutrient DWS has `Idempotency-Key` only on async `POST /build` with `Prefer: respond-async`; `/processor/*` and `/extraction/*` have none. Baseline = client-side dedupe ledger keyed on plan token for eSign sends; per-operation digest (document SHA-256 + instructions) for Nutrient.**
 
**Aug 19 — ~2h, from the recovered slack. GATE 0: prove eSign exists for us.** ✅ DONE Aug 18, a day early (recovered slack pulled it in, like the first batch) — see `docs/gate0-aug18.md`, fixtures in `docs/fixtures/`.

- **Foxit eSign: 5/5 PASS.** Entitled (HTTP 200), `createfolder(sendNow:false)` returns `folderId`, `folderStatus` confirms `DRAFT`, and a **send-draft route exists on the gateway** — **locked as the send host** (same `client_id`/`client_secret` headers, no OAuth token). Legacy host exists as fallback. eSign shares the PDF Services credential pair. **Aug 20–22 core work proceeds.**
- **Nutrient Data Extraction: BLOCKED.** `POST /extraction/parse` → **403**: Data Extraction is a separately provisioned product; our DWS Processor key is not entitled. **UNBLOCK (human, ~5 min): add `NUTRIENT_DWS_EXTRACTION_API_KEY` to `.env` from dashboard.nutrient.io.** Probe ships fixture-ready (`node mcp/nutrient/extraction-probe.mjs`); the 403 diagnostic is the committed fixture. Key is `pdf_live_` = **live**, so the async-`/build`-test-key caveat does not apply.
- **`safe-write-mcp-core` cloned** at `../safe-write-mcp-core`. Critical-path repo is in place.
 
**Aug 20–22 — ~14h. Core v0.2. The MUST list.** ✅ DONE Aug 19, a day early — see `safe-write-mcp-core` [PR #15](https://github.com/jpka/safe-write-mcp-core/pull/15) (`Closes #14`), merged as `0cbe5e7`.
- Split `consume()` into `beginExecute()` → `confirmExecuted()` / `confirmFailed()`. `"executed"` is only audited after host confirmation. A plan stuck in `executing` past a timeout becomes a queryable state, not a silently forgotten one. **Done**: `consume()` kept as a documented, explicitly-not-crash-safe legacy wrapper; `listExecuting()` surfaces stuck plans.
- Durable journal: append-only, fsync'd, one line per token transition, so restart can detect "mid-execute." **Done**: `src/journal.ts`, streamed replay (no whole-file read), 0o600 perms, parent-directory fsync so a fresh journal survives a power loss, broken-descriptor guard on partial/serialize write failures.
- Plan token as documented idempotency key. *(Confirmed Aug 18: eSign has no server key — the ledger IS the idempotency.)* **Done**, documented on `PlanCreated`/`beginExecute`.
- **Reconciliation is a host-supplied callback, not a vendor call.** *(Revised Aug 18, send host locked Aug 18.)* The ledger contract is host-independent, but `confirmExecuted()`'s reconciliation is not. The core takes a `reconcile(token) → 'done' | 'not-done' | 'unknown'` function from the host. Foxit's implementation is the **gateway** `folderStatus DRAFT/SHARED` check via `GET /esign/api/v1/folders/myfolder?folderId=`, confirmed reachable in Gate 0 (legacy host is the fallback). **Core done**: `PlanStoreOptions.reconcile`, bounded by `reconcileTimeoutMs` so a hanging host hook can't stall recovery. The Foxit-specific `reconcile` implementation still lands with the eSign adapter (Aug 23–25).
- Core-enforced `dataDigest` re-check. **Done**: `beginExecute()` fails closed with `DATA_DIGEST_MISMATCH`.
- Tests for each, against **fixtures from Gate 0**, not the live API. Keep the suite green — 73/73 passing is a credibility asset in the repo. **Done**: 108/108 passing (73 pre-existing + 35 new).

*Delegated to `opencode run -m opencode/deepseek-v4-flash-free` across three rounds (initial implementation, then two CodeRabbit review-fix rounds), reviewed and merged by Claude Code.*
 
**Aug 23–25 — ~12h. The eSign adapter and agent loop.** ✅ DONE Aug 20, three days early — see PR #7 (`feat/esign-mcp-server-and-agent`).
- MCP server (`mcp/foxit/esign-mcp-server.mjs`) exposes the crash-safe lifecycle as tools: `esign_create_draft`, `esign_begin_send`, `esign_confirm_executed`, `esign_confirm_failed`, `esign_list_executing`, `esign_reconcile`.
- Approval server runs **in-process** (shares the PlanStore), with a custom `renderPlan` hook: folder name + recipient list + explicit irrevocability warning — NOT `JSON.stringify` on the raw payload (which would leak PII).
- Custom renderPlan hook renders recipients + folder name + irrevocability warning instead of the core's `JSON.stringify` default.
- Fixed `beginEsignSend` to require the payload explicitly (core can't reconstruct it from the journal).
- Fixed `confirmFailed` to refuse when folder status is already SHARED (prevents double-send).
- Fixed `loadEsignStore` to not depend on missing `PlanStore.fromJournal` (constructor already replays the journal).
- 7/7 tests pass against Gate 0 fixtures (no live API).
- Fixed broken import path (`src/index.js` → correct `dist/index.js` relative path).
 
**Aug 26–28 — ~12h. Nutrient stage.** ✅ **DONE Aug 20, six days early — merged as `799e46c` (PR #10). See `docs/nutrient-stage-aug20.md`.** The blocker cleared (Data Extraction key in `.env`), so extraction routing, staged redaction, the Nutrient MCP server, and 97 new tests all landed. 104/104 tests pass repo-wide. Five review rounds, 19 findings fixed, 1 declined as upstream and filed as core [#20](https://github.com/jpka/safe-write-mcp-core/issues/20).

**Plan correction, Aug 20: the Gate 0 probe was pointed at the wrong endpoint.** `/extraction/parse` returns layout elements with one composite `confidence` each. The match labels and `confidenceComponents` this whole routing design rests on live only on **`/extraction/extract`**, the schema-based endpoint, under `output.metadata`. `/parse` could never have calibrated the gate. `mcp/nutrient/extract-probe.mjs` is the probe that can; the old one stays as the entitlement record.

Three findings from the live API, in ascending order of how much they matter:

1. **`not_found` fields are invisible to the documented walk.** An ungrounded field is omitted from `output.data` while its citation stays in `output.metadata`. Nutrient's own published `iter_citations` example walks `data` — so it structurally cannot see the single most important routing signal. We walk the union of both.
2. **Every grounding signal cleared two wrong numbers.** `understand` mode returned `total_amount: 26.86` (document: $86.86) and `tax_amount: 5.27` (document: $5.87), both at `match: id_match`, `confidence: 0.970`, `groundingScore: 0.95`. The only signal that dissented was `recognitionScore` (0.678, 0.569 — the two lowest on the page). The grounding signals answer "is the value where the model says it is", which was true; they cannot answer "were the glyphs read correctly", which is what failed. This is the "confidently wrong even in the highest-accuracy mode" case Nutrient warned about, reproduced on the first live call.
3. **Agentic mode is the most dangerous mode for a gate, not the safest.** It fixed the totals and broke the line-item table instead ("Parcel 1" → description `"Parcel"`, quantity 1 — the row number shifted into the quantity column). Two wrong fields either way. And being VLM-augmented, it omits `recognitionScore` entirely, so on the raw signals 14 of 16 fields auto-approve including both wrong ones. **Double the cost bought better answers and less ability to tell whether they were right.** Hence `requireRecognition` per document type: absence of an OCR score is not evidence of quality.

Verified property, asserted per mode against committed fixtures: **no field whose value disagrees with the document is ever auto-approved.** Redaction is `createRedactions` (staged, unattended) then `applyRedactions` behind `alwaysRequireApproval: true` — one approval queue, two irreversible actions. Confirmed by decompressing the PDF streams that the *staged* document still contains the PII and only the applied one destroys it: the staged file looks redacted and is not, which is precisely the gap the gate guards.

Two limits documented rather than papered over: `/build` has no server-side state, so `reconcile` returns `"unknown"` honestly and a plan interrupted mid-apply stays visibly `executing` for a human (guessing either way would either double-apply or put a false claim in the audit log); and `/build` offers no synchronous idempotency, so the per-operation digest is both our dedup key and the core's `dataDigest` TOCTOU check.

Also found and fixed a real bug: `store.beginExecute()` takes the current digest as a **third argument** and treats a missing one as a mismatch, so every redaction apply failed `DATA_DIGEST_MISMATCH` until the adapter passed it. The tests caught it. Checked the eSign adapter for the same class of error — it passes `dataDigest: null`, so its two-argument call is correct.

*Remaining Nutrient work: thresholds are all marked `calibrated: false` and need a representative sample per document type, not one synthetic invoice. A test asserts none of them claims calibration it has not had.*
 
 **Aug 29–30 — ~12h. Foxit literal-compliance + audit polish (Aug 27 revision — was ~8h audit/UI).** ~~1/3 done.~~ ~~Drop raw `payload` from `GET /api/plans` — right now a host's careful `renderPlan` redaction is silently bypassed by that endpoint, which is embarrassing in a PII demo.~~ **DONE Aug 20** via `safe-write-mcp-core@0.3.0` (issue #18, PR #19 — drops `payload` by default, opt-in via `exposeRawPayload`). Consumed here by version bump. **Aug 23: hash-chained JSONL audit sink DONE** (`mcp/lib/jsonl-audit-sink.mjs` — fsync'd per-line `prevHash`+sha256 chain for both stages, streamed tamper verification, torn-tail truncation on recovery; also fixed the eSign audit sink corrupting MCP stdout with `console.log`, and added deterministic crash injection `NO_UNDO_CRASH_AFTER_FSYNC`; CI now runs the full suite, 114 tests).

 Remaining Aug 29–30 work, in dependency order:

 - **(A) Plain-prompt parser — 2h.** New `mcp/foxit/prompt-parser.mjs` (zod schema + LLM fallback): `prompt string → {folderName, recipients, docSource}`. `agent/esign-agent-loop.mjs` will gain `runFromPrompt(prompt)` that feeds the existing `runAgentLoop` at `agent/esign-agent-loop.mjs:121`; approval card echoes the prompt excerpt + parsed fields for correction. Tests against 3–4 fixture prompts with fixture seam (no live LLM in CI); no new creds. Until it lands, structured `runAgentLoop` remains the tested entry.
 - **(B) Wire real Foxit PDF assembly — 4h.** Replace `tinyPdf` stub at `mcp/foxit/esign-adapter.mjs:createEsignFolder` (`tinyPdf` const, ~line 435) with one genuine MCP call: render invoice data as HTML, then `pdf_from_html` → `get_task_result` via `mcp/foxit/call-tool.mjs` stdio (confirmed present in catalog; `pdf_generation` does NOT exist — Aug 27 contact) whose bytes become `base64FileString` for `createfolder` and whose SHA-256 becomes `extra.documentSha256`. Single-credential repro (`FOXIT_CLIENT_ID/SECRET` only; optional `FOXIT_CLOUD_API_HOST`). Fixture: committed `mcp/foxit/.esign-journal.jsonl` excerpt + `get_task_result` transcript; signed artifact not required for CI.
 - **(C) Signed-document retrieval — 2h, gated on probe.** First extend `mcp/foxit/esign-probe.mjs` with PLANNED `--probe-download` (currently only `--create-draft` at line 42): after self-sign with two owned inboxes, hit `GET /esign/api/v1/folders/document/download?folderId=&docNumber=` (single-document route) and `GET /esign/api/v1/folders/download?folderId=` (envelope), and confirm the post-sign state matches what the `folder_executed` webhook fires on — routes vendor-named Aug 27 (`docs/foxit-contact-aug27.md`). Then extend `mcp/foxit/esign-adapter.mjs` with `pollUntilSigned`/`downloadSignedDocument` reusing `checkFolderStatus` (~line 251) / `handleWebhookEvent` (~line 650) with 30s timeout/`transportError` handling per `mcp/foxit/esign-adapter.mjs:77-83` and fixture-seam tests on committed SHARED fixture. `agent/esign-agent-loop.mjs` polls after `SHARED` and writes `signed-{folderId}.pdf`; audit sink records `signed_retrieved`. Until probe confirms download route + terminal state, cut-list #1 applies (ends at `sent for signature`) — and `Sep 1–2` video's "signed PDF on disk" shot is gated on this probe (see S6).
 - **(D) Single-pipeline wiring (P6) — 2h.** Collapse Nutrient + Foxit onto one `PlanStore`/approval server when `NUTRIENT_DWS_EXTRACTION_API_KEY` is present, but keep Foxit-only path when it is not — fixes the `docs/showcase.html:§ Architecture` "What 'planned' means here" gap (warn block at 528-532) for the Foxit track; **D ships only with a matching `docs/showcase.html` + `docs/demo-video-script.md` prerequisite-gate update** (that page still warns "two arrows are design not code" until D lands). Keep `journalPath` as single source of truth; namespace durable files beside journal (`.token-map.json`, `.webhook-dedup.json` at `mcp/foxit/esign-adapter.mjs:212-217`) to avoid Nutrient/PDf-stage collision — share `createEsignStore`/`loadEsignStore` explicitly.
 - **(E) Approval UI polish — 2h.** Card renders prompt excerpt + assembled doc preview + `documentSha256` digest + irrevocability warning; no `JSON.stringify`. This is cut-list #6.

*Aug 20 correction was: the `/api/plans` item is upstream in `safe-write-mcp-core`, fixed by core patch + version bump. That patch landed as v0.3.0 and is consumed here. Servers A and C have the same leak and inherit the fix on their next bump — file a reminder issue if they aren't bumped before submission.* The other two items are genuinely local and unaffected.

 **Aug 31 — feature freeze.** README, architecture diagram, setup instructions that a judge can actually follow. README now documents two repro paths: **Foxit-only (one credential pair)** and **full pipeline (Foxit + Nutrient)**; architecture diagram shows prompt → Foxit PDF (reversible) → gate → eSign (irreversible, direct API) → signed PDF, with MCP boundary explicitly drawn.
 
 **Sep 1–2 — ~8h.** Demo video, 2–4 min, crash shot + prompt-to-signed-document shot. Devpost project page has a one-paragraph Foxit boundary defense (`signing outside MCP because the catalog is reversible by design`). Enter it to Foxit, Nutrient, and Overall. One line each on where their API did the real work. **Foxit entry video must show: prompt typed → Foxit assembly (MCP tool call) → approval card with irrevocability warning → send → signed PDF on disk.**
 
 **Sep 3, before 2pm ART.** Submit. Not at 1:55.
 
 **Cut list, in order, if time runs short (Aug 27 revision):** (1) Signed-document retrieval polling (keep send, defer auto-download — still satisfies "sent for signature" but weaker literal; probe-dependent; download routes now vendor-named Aug 27, `docs/foxit-contact-aug27.md`) → (2) Single-pipeline collapse P6 (keep two stores, document that Foxit-only is the judged path) → (3) N-of-M approval → (4) approval-server authentication (upstream #20) → (5) Nutrient redaction (keep extraction-with-confidence, which already satisfies Nutrient's "meaningfully" bar per `docs/nutrient-stage-aug20.md:130-156` — redaction is second irreversible action but not required for that track) → (6) polished approval UI preview. **Never cut:** the crash-safety fix, the real Foxit PDF wiring (stub → real MCP call), the plain-prompt entry point, or the demo video. Rationale: the first two are what the Foxit rubric grades — a stub document or a structured-JSON entry point reads as "did not use the MCP server as intended."
 
 *Aug 20 note on approval-server authentication, now that it has a filed home:* CodeRabbit raised it on PR #10 and it is real — `startApprovalServer` binds loopback and checks `Host`/`Origin`/`Sec-Fetch-Site`, but carries no shared secret, so any local process that sends the expected headers can approve a plan. It is **upstream**, filed as `safe-write-mcp-core` [issue #20](https://github.com/jpka/safe-write-mcp-core/issues/20), and A and C inherit it too. Deliberately left on the cut list rather than fixed downstream: patching around it in `no-undo` would leave the other two servers exposed and triplicate the fix. If a judge asks about it, the honest answer is that the threat model the header checks cover is a malicious browser page, not a hostile local process, and the gap is tracked in the core with a proposed per-session bearer token.
 
**Contingency, if Gate 0 says eSign is not available to us** *(added Aug 18; full version in `docs/review-aug18.md` §5)*. Decide within one hour, ranked:
1. Separate eSign trial signup, if one exists. Changes nothing else.
2. **Re-target the irreversible action at PDF Services' destructive tools** — `delete` of an uploaded document, password-`protect`. Practically irreversible, still squarely on-brief for a challenge titled *"Your Agent Shouldn't Sign That"*, keeps all three entries and reuses every line of core work. This is the one to reach for.
3. Nutrient-only plus Overall, with the gate on redaction (destructive by construction). Drops the Foxit track.
 
---
 
## Carry-over value

Everything here is an asset you needed anyway:

- `safe-write-mcp-core` v0.2 is strictly better after this, and A and C both inherit the fix — **A currently has this same crash-safety gap** in any deployment where the write isn't purely transactional.
- The demo video is the Loom your Upwork Project Catalog gallery is blocked on. "Rejected deletion, agent adapts" becomes "rejected signature, agent adapts." The Foxit prompt-to-signed-document cut is the Loom thumbnail.
- A public Devpost page and a judged placement is third-party proof your listing cannot otherwise buy.
- Three shipped servers on one core reads as a designed pattern. Two reads as two one-offs. The Aug 27 pivots make the Foxit pipeline independently reproducible with one credential pair — the strongest portability demo for the core.
 
---
 
## Open items

- ~~**⚠ Human action (unblocks Aug 26–28): get a Nutrient Data Extraction API key**~~ **RESOLVED Aug 20.** `NUTRIENT_DWS_EXTRACTION_API_KEY` is in `.env` and live; the Nutrient stage is built and tested against it. Live transcripts committed in `docs/fixtures/nutrient-extract-{structure,understand,agentic}-*.json`.
- **Foxit literal-compliance pivots (added Aug 27 — five items, ~12h, § Aug 29–30 A–E; time-budget fixes B1/B2):**
  - **(A) Plain-prompt parser** — PLANNED `mcp/foxit/prompt-parser.mjs` + `agent/esign-agent-loop.mjs:runFromPrompt` (will gain `--prompt` arg, feeding existing `runAgentLoop:121`). Risk: LLM parse variance → mitigated by zod + fallback regex + approval-card correction before gate. Estimate is 2h with local-regex default and fixture-seam tests (no live LLM in CI); if LLM is primary, buffer to 3h (see S5). Until it lands, structured `runAgentLoop` remains tested entry.
  - **(B) Real Foxit PDF wiring** — replace `tinyPdf` stub at `mcp/foxit/esign-adapter.mjs:createEsignFolder` (~line 435) with live MCP `pdf_from_html` (render invoice data as HTML first; `pdf_generation` does NOT exist in the catalog — Aug 27 contact, `docs/foxit-contact-aug27.md`) → `base64FileString` via `mcp/foxit/call-tool.mjs` stdio → `get_task_result` transcript, SHA-256 → `extra.documentSha256`. Risk: task polling latency → reuse existing `get_task_result` pattern from Gate 0 smoke test. Must keep fixture seam so CI passes without creds. Single-credential aliases share one secret (see Pipeline step 3).
  - **(C) Signed-document retrieval** — gated on PLANNED probe `node mcp/foxit/esign-probe.mjs --probe-download` (to be added; currently only `--create-draft`) confirming the vendor-named download route (`GET /esign/api/v1/folders/document/download?folderId=&docNumber=` / `download?folderId=`, after `folder_executed` webhook — Aug 27, `docs/foxit-contact-aug27.md`) + terminal post-sign state + self-sign poll window; then `pollUntilSigned`/`downloadSignedDocument` in `esign-adapter.mjs` reusing `checkFolderStatus` (~251) / `handleWebhookEvent` (~650, dedups `folder_executed`) with 30s timeout/`transportError` handling (77-83) and fixture-seam tests on SHARED fixture. Risk: signer delay → poll with timeout, audit `signed_retrieved`, demo uses self-sign inboxes. Cut-list #1 until probe lands.
  - **(D) Single-pipeline P6 collapse** — one `PlanStore` when Nutrient present, Foxit-only otherwise; fixes `docs/showcase.html:528-532` § Architecture warn block (and updates that page + `docs/demo-video-script.md` prerequisites when D lands). Risk: journal path coupling → keep `journalPath` as single source of truth; namespace durable files (`.token-map.json`/`.webhook-dedup.json` at `mcp/foxit/esign-adapter.mjs:212-217`) and share `createEsignStore`/`loadEsignStore` explicitly.
  - **(E) Approval UI polish — 2h** — card renders prompt excerpt + doc preview + `documentSha256` digest + irrevocability warning; no `JSON.stringify`. Cut-list #6.
- **Calibrate the thresholds against real documents.** Every entry in `THRESHOLDS` is marked `calibrated: false`, and a test asserts none of them claims otherwise. Setting them needs a representative sample per document type — one synthetic invoice is enough to prove the routing works, not enough to tune it. `node mcp/nutrient/extract-probe.mjs --calibrate` prints the table to set them from. Budget an hour before the freeze. *Foxit-only path does not block on this.*
- **Worth sending Jon Addams**, who offered to look at responses where routing felt wrong: `confidenceComponents.groundingScore` reported 0.95 on `total_amount: 26.86` where the document reads $86.86, at `match: id_match` and `confidence: 0.970`. `recognitionScore` (0.678) was the only dissenting signal. Also worth asking whether agentic mode can be made to emit `recognitionScore`, since its absence is what forces every agentic field to human review.
- ~~**Confirm the built-in redaction preset names**~~ **DONE Aug 20.** Probed live against `/build`: 11 valid (`email-address`, `social-security-number`, `credit-card-number`, `north-american-phone-number`, `international-phone-number`, `date`, `time`, `url`, `ipv4`, `ipv6`, `vin`), and the guessable short forms `email` / `phone` / `ssn` / `phone-number` all return 400. Pinned as frozen `CONFIRMED_PRESETS`, surfaced in the MCP tool schema, tested. This mattered more than it looked: a preset that matches nothing stages zero regions and still returns a valid PDF, so a typo produces a document that looks redacted and is not.
- **Devpost Foxit defense paragraph (new, ~30 min).** One paragraph arguing why signing stays outside MCP (reversible catalog vs irreversible send) — the prompt the challenge explicitly invites. Lives in README + Devpost; no code. *Aug 27: Foxit's Jason Welch confirmed this pattern in writing (`docs/foxit-contact-aug27.md`) — with his permission, one attributed line strengthens the paragraph.*
- **⚠ Pending sponsor answer (Aug 27):** asked Jason whether a local hackathon demo can rely on polling `GET /esign/api/v1/folders/document/download?folderId=&docNumber=` as primary (since `folder_executed` needs a public HTTPS endpoint) or should stand up the webhook even locally. Doesn't block C — polling `myfolder`/`document/download` is the fallback either way — but the answer decides whether the webhook path ships in the demo.
- Apptio, useBruno, and Wundergraph have not published their challenges. Wundergraph (GraphQL federation gateway) is the one most likely to fit a "safe write gateway" story. Re-check `/details/sponsors` around Aug 25 — a fourth free track is worth ten minutes of checking.
- Foxit's listed contact email on the sponsor page has a typo (`...foxitsoftware.come`). Use the developer portal if you need support.
- Two eSign probe drafts (`35426242`, `35426627`) are sitting in the eSign dashboard, unsent — delete when convenient.
- Team size is 1–5. Solo is fine; a second person on the demo video would not hurt.